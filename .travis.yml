sudo: required

services:
- docker

language: python

python:
- '3.7'

before_install:
- sudo apt-get -y update || true

install:
- dev/setup_venv.sh

before_script:
- dev/start_infsnmp3_dependencies.sh

script:
- dev/all_tests.sh

notifications:
  email:
    - bifer@alea-soluciones.com
